#LINK_DIRECTORIES(${CMAKE_BINARY_DIR})

SET(SS_SENSOR_DI_DIR "${SS_BASE_DIR}/SoftSensorPlugin/DiscomfortIndexSensor")

INCLUDE_DIRECTORIES(${SS_SENSOR_DI_DIR}/include)
INCLUDE_DIRECTORIES(${SS_CORE_DIR}/include)
INCLUDE_DIRECTORIES(${SS_CORE_DIR}/src/SSMInterface)

SET(EXTRA_CXXFLAGS "-std=c++0x -DLINUX")
SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${EXTRA_CXXFLAGS}")


FILE(GLOB OBJ_DI_SRCS ${SS_SENSOR_DI_DIR}/src/*.cpp)

ADD_LIBRARY(${SS_SENSOR_DI} SHARED ${OBJ_DI_SRCS})
INSTALL(TARGETS ${SS_SENSOR_DI} DESTINATION ${LIB_INSTALL_DIR})

INSTALL(FILES HighContextDictionary.xml DESTINATION ${SHARE_INSTALL_PREFIX}/oic)

